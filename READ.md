# nft-mini
NFT Collection (devnet) minted with Umi/Metaplex.  
Scripts: mint, verify (sized), audit via DAS, read on-chain.

## Prerequesite
- Node.js >= 18 (v22 OK)
- `ts-node`, `typescript`

## Configuration
Create `.env` from `.env.example`:

